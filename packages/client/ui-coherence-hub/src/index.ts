/** Host loader entry for the Coherence Hub browser implementation exported from `./client`. */

/** Host plugin body — no host-side behavior for the Coherence Hub view. */
export function apply(): void {}
