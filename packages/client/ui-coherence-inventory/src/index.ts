/** Host loader entry for the package-groups browser implementation exported from `./client`. */

/** Host plugin body — no host-side behavior for the package-groups tab. */
export function apply(): void {}
